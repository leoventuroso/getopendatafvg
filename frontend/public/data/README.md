# Dataset statici (placeholder)

Questa cartella ospita i file statici letti dal client:

- `base_layers.pmtiles`: layer vettoriali di base (rete stradale, sentieri, rischio, NDVI).
- `community_data.duckdb`: database embedded per dati crowdsourced.

In Fase 0 i file reali non sono inclusi nel repository.
